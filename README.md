# laravel-mini-erp
A responsive Laravel + SQLite mini ERP featuring Products, Inventory movements, Orders workflow, and a mock marketplace sync (JSON import + local status log) with migrations, seeders, and Form Request validation.
